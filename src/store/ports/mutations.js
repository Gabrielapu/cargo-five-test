const mutations = {
  'SET_PORTS'(state, ports) {
    state.ports = ports
  },
  'SET_LINKS'(state, links) {
    state.links = links
  },
  'SET_META'(state, meta) {
    state.meta = meta
  }
}

export default mutations